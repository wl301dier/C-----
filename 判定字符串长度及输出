/*比较两个字符串长度。如果第一个字符串与第二个字符串长度相等，那么输出第一个字符串；
 *如果第一个字符串比第二个字符串长度长，那么输出第一个字符串；
 *否则输出第二个字符串
 */
#include<stdio.h>

void main(){
	char str[40],strt[40];
	printf("请输入字符串1：");
	gets(str);
	printf("请输入一个字符串2：");
	gets(strt);
	for(int i=0;str[i]!='\0';i++);
	for(int j=0;strt[j]!='\0';j++);
	if(i==j){
		printf("%s",str);
	}else if(i>j){
					printf("%s",str);
			}else{
				printf("%s",strt);
			}		
}
